const { authRoutes } = require('./auth');
const { userRoutes } = require('./users');
const { basketballRoutes } = require('./basketball');

module.exports = {
  authRoutes,
  userRoutes,
  basketballRoutes,
};
